/*
import { v4 as uuidv4 } from 'uuid';

export class Wishlist {
  // Fields
  idWishlist: string;
  clienteId: string;
  produtos: Array<{ produtoId: string; quantidade: number }>;

  // Constructor
  constructor(clienteId: string, produtos: Array<{ produtoId: string; quantidade: number }>) {
    this.idWishlist = uuidv4();
    this.clienteId = clienteId;
    this.produtos = produtos;
  }

  // Getters
  getIdWishlist(): string {
    return this.idWishlist;
  }

  getClienteId(): string {
    return this.clienteId;
  }

  getProdutos(): Array<{ produtoId: string; quantidade: number }> {
    return this.produtos;
  }

  // Setters
  setIdWishlist(idWishlist: string): void {
    this.idWishlist = idWishlist;
  }

  setClienteId(clienteId: string): void {
    this.clienteId = clienteId;
  }

  setProdutos(produtos: Array<{ produtoId: string; quantidade: number }>): void {
    this.produtos = produtos;
  }
}
*/